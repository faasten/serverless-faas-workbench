[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "linpack"
version = "0.0.1"
dependencies = [
  "numpy"
]

[tool.hatch.build.targets.wheel]
packages = ["."]
